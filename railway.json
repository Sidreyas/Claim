{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1 --threads 4",
    "healthcheckPath": "/get_claims",
    "restartPolicyType": "ON_FAILURE"
  }
}
